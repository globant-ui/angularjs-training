'use strict';

describe('Service: incomeService.js', function () {

  // load the service's module
  beforeEach(module('myAppApp'));

  // instantiate service
  var incomeService.js;
  beforeEach(inject(function (_incomeService.js_) {
    incomeService.js = _incomeService.js_;
  }));

  it('should do something', function () {
    expect(!!incomeService.js).toBe(true);
  });

});
